# fashion-classifier-app
A deep learning-based image classification web application built with Flask and TensorFlow. The model is trained on the Fashion-MNIST dataset and deployed as a simple web interface that allows users to upload images and receive real-time clothing category predictions with confidence scores.
